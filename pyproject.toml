[project]
name = "word-frequency-counter"
version = "0.1.0"
description = "Simple word frequency counter CLI and library"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
]
